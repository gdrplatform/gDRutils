# get optional colData fields

get optional colData fields

## Usage

``` r
get_optional_coldata_fields(se)
```

## Arguments

- se:

  a SummarizedExperiment object with drug-response data generate by gDR
  pipeline

## Value

a charvec containing the names of the optional identifiers in the SE
colData
